<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- 扫码（zxing 库自身也声明 CAMERA，这里显式声明便于审阅） -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- 精确闹钟（课前提醒准点弹出）：
         API 31-32 用 SCHEDULE_EXACT_ALARM（默认授予、用户可撤销）；
         API 33+ 用 USE_EXACT_ALARM（日历/闹钟类应用自动授予；本应用仅侧载分发，不受 Play 审核约束）。 -->
    <uses-permission
        android:name="android.permission.SCHEDULE_EXACT_ALARM"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@style/Theme.Timetable">

        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|screenSize|keyboardHidden|uiMode"
            android:exported="true"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 竖屏扫码页（zxing 库 CaptureActivity 默认锁横屏，此处覆写为竖屏；主题用库自带） -->
        <activity
            android:name=".VerticalScannerActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:stateNotNeeded="true"
            android:theme="@style/zxing_CaptureTheme"
            android:windowSoftInputMode="stateAlwaysHidden" />

        <!-- 开机 / APK 升级后立即补排一次提醒计划（WorkManager 周期任务本身也会自续） -->
        <receiver
            android:name=".BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!-- 课前提醒闹钟触发器（仅应用内 PendingIntent 触发，不对外暴露） -->
        <receiver
            android:name=".AlarmReceiver"
            android:exported="false" />
    </application>
</manifest>
